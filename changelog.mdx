---
title: "Changelog"
description: "Vectoree Cloud release notes — user-facing changes only."
---

Product release notes for Vectoree Cloud. Newest first. This page does not list internal git tags, commit hashes, or diffs.

---

## 1.0.1 — 2026-09-23

Vectoree Cloud 1.0.1 is here. This release improves local agent setup, sharpens console onboarding and product feedback, and adds marketing tools to Tool Hub, so you can take an agent from local development to the cloud in one flow.

### 🚀 New features

* **First-run guide (Onboarding)**
  * Sign in and land straight in the console. Connect an existing local project, or start from a template.
  * Get setup prompts for coding agents such as Cursor, Claude Code, and Codex.
  * A successful project link unlocks **$5 in welcome credit**.

* **Tool Hub marketing tools**
  * Similarweb, Semrush, and Ahrefs are in. Your agent can call traffic, competitor, and SEO data directly.
  * No separate third-party accounts for these tools.
  * More plug-in tools will keep arriving.

* **Experience and feedback**
  * **Homepage FAQ and marquee:** seven core FAQs cover positioning, billing, and code compatibility. Partner logos now run as a marquee.
  * **Feedback, end to end:** the site and the console accept rich text and screenshots, including bugs and feature ideas.

### 🛠️ Fixes

* **Search usage lands on the right org:** hosted `web_search` and `web_fetch` now carry the organization ID, so billing is accurate.
* **Object storage:** object keys that include a path open correctly.

---
> 💡 **What's next:** Deploy and the Payments SDK are iterating fast. More Tool Hub data tools will keep launching.

## 1.0.0 — 2026-09-04

Vectoree Cloud 1.0 is the first public release: an out-of-the-box cloud backend built for AI agents. Write your agent locally in Cursor, Claude Code, or Codex, connect in seconds, and get the full cloud stack — no more glue code across five SaaS tools.

**Features**
- **Skill + CLI (the agent's cloud control surface):** run `npx skills add VectoreeAI/vectoree-skills`, then `login` / `link`, so your local coding agent can operate the cloud project directly
- **[Model Gateway](/models) (all-in-one model gateway):** one API key for 36+ leading models (OpenAI, Anthropic, and more), with an OpenAI-compatible API, Model Plaza, `vectoree/auto` routing, plus TTS / STT, image, video, and embeddings
- **Tool Hub (hosted MCP):** out-of-the-box hosted MCP for Web Search and Page Extract, billed from the org wallet — no extra third-party API accounts to manage
- **[Authentication](/auth) (login for your app):** add email OTP and Google sign-in for users of *your* agent app, including multi-tenant sessions
- **[Database](/database) (Postgres per project):** a dedicated PostgreSQL database per project; manage tables, schema, and SQL from the dashboard or CLI
- **[Storage](/storage) (object storage):** buckets with public access or signed private access
- **[PAYG wallet](/billing) (pay as you go, no monthly plan):** Stripe top-up, no forced subscription; model and Tool Hub usage deducts from the org balance

> 💡 **Launch offer:** Auth, Database, and Storage run on shared infrastructure in this launch — free to use, not billed separately.
>
> 📌 *Note: Deploy and in-app Payments are iterating fast and are not in 1.0.*
