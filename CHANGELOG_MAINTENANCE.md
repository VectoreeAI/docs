---
title: Changelog 维护规范
---

# Changelog 维护规范（Vectoree Docs）

> 适用仓库：`VectoreeAI/docs`（Mintlify 文档站源）  
> 维护对象：`changelog.mdx`（仓库根目录）  
> 版本：2026-09-01

## 1. 背景

- 产品文档（docs）接入 Changelog 时，发现项目仓库中已存在一个 `changelog.mdx` 文件——即线上 Changelog 页面本身由该文件驱动。
- 该文件目前处于**手工维护**状态，没有接入任何自动生成流程。

## 2. 关联链路与现状

- 文档站通过「`push` 到 `main` → Mintlify webhook 自动同步」上线，`changelog.mdx` 的内容直接决定线上 Changelog 页面展示什么。
- 该文件**不会**从 git commit 自动抓取更新：当前无 `<Changelog/>` 组件、无 CI 自动脚本。文件里写什么，线上就显示什么。
- 因此，Changelog 能否持续更新，完全取决于是否有人往文件里写内容——它是一个"有人改才有、没人改就停"的人工文档。
- 此前页面出现过大量空白条目（`No documented features or fixes in this tag.`）与失效的 `Full diff →` 链接（点击 404），正是因为长期无人维护、且早期某次自动生成的渲染结果被冻结成静态文本后未再更新。

## 3. 方案：让 Changelog 可持续维护

- **目标**：Changelog 能随每次发版持续更新，而不是积压后集中补、或长期空白。
- **核心做法**：研发在提交代码（合入 MR / 发版）时，若该次变更对用户可见，应**同步提交对应的 Changelog 内容**（或提供变更说明，由文档维护人整理入文件）。
- **为什么是"提交代码时"**：变更与记录同频发生，能避免事后回忆"这版到底改了啥"，也大幅降低文档维护人逐版本追溯的成本。Changelog 的生命力来自每次发版都有人把它写进去，而不是依赖某个人事后统一补。

## 4. 更新时机与位置

- 每次发布新版本（打 tag / 发版）且变更对用户可见时，新增**一条**记录。
- 最新版本写在文件**最顶部**（倒序排列：新的在上，旧的在下）。**不要修改历史记录。**
- 路径：仓库根目录 `changelog.mdx`。流程：本地修改 → `commit` → `push` 到 `main` → Mintlify 自动拉取重建上线。

## 5. 条目格式（模板）

```markdown
## <版本号> — <发布日期 YYYY-MM-DD>

**Features（新功能）**
- 一句话用户视角描述：用户现在能做什么

**Bug Fixes（问题修复）**
- 一句话用户视角描述：之前什么不好用、现在修好了

**Improvements（优化，可选）**
- 性能 / 体验上的改进
```

示例：

```markdown
## v2.1.0 — 2026-04-01

**Features**
- 支持一键从 GitHub 克隆模板项目，降低上手门槛

**Bug Fixes**
- 修复管理员令牌刷新偶发失败的问题
```

## 6. 写什么 / 不写什么

✅ **要写**

- 用户能感知的变化，用**用户视角**的一句话描述（不是"改了哪个函数"，而是"用户现在能做什么 / 之前什么不好用现在修好了"）。
- 语言与文档站保持一致（英文）。如先用中文起草，由文档维护人统一翻译后再入文件。

❌ **不要写**

- 原始 commit message、commit hash（如 `773e0df`）—— 对读者无意义，且不可点击。
- `Full diff →` 这类版本对比链接 —— 当前仓库路径 / tag 未对齐，点击会 404。
- 内部术语、技术实现细节、Jira / MR 编号（除非对用户有直接价值）。

## 7. 协作流程

- **研发（责任人）**：发版 / 提交相关 MR 时，若变更对用户可见，按第 5 节模板将"用户视角一句话变更列表"（Features / Bug Fixes / Improvements 归类）写入 `changelog.mdx` 顶部，**随业务代码一同提交并推送**。
- **文档维护人（可选协助）**：如代为整理内容，按模板排版、翻译为英文后，作为一次普通文件改动提交即可——这不是独立的"发布"动作。
- **同步机制**：只要 `changelog.mdx` 在 `main` 分支被更新（无论由谁提交），Mintlify 的 webhook 会自动拉取并重建线上 Changelog 页面，**没有额外的人工发布步骤**。

## 8. 历史遗留与后续可选项

- **空白条目**：历史上 `No documented features or fixes in this tag.` 是早期自动生成组件冻结下来的默认占位文本，不要复用。若某版本确实无用户可见变化：整条可省略，或写一句通用说明 `General improvements and bug fixes.`
- **恢复 diff 链接**：需先确认产品代码仓库在 GitHub 的真实路径（当前写的是 `Vectoree/vectoree`，但组织是 `VectoreeAI`）+ 版本 tag 已 `git push --tags` 推上去了。条件满足后再将对比链接加回。
- **改为自动生成**：可改用 Mintlify `<Changelog/>` 组件，由 git tag 自动出记录；前提是 Mintlify 连接的仓库有版本 tag 且 commit 使用 conventional 前缀（`feat:` / `fix:` 等）。此项需与 leader 确认后再切换，且会与本节"研发手写"的维护方式互斥。
